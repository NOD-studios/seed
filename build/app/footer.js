(function(window, require, define) {
  'use strict';
  return define([
    'app'
  ], function(app) {
      app.footer = {
        name : 'app/footer',
        init : function() {}
      };
      return app;
  });
}) (window, require, define);
