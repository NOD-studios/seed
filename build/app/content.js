(function(window, require, define) {
  'use strict';
  return define([
    'app'
  ], function(app) {
      app.content = {
        name : 'app/content',
        init : function() {}
      };
      return app;
  });
}) (window, require, define);
