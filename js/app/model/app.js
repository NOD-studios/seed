(function (window) {
  'use strict';
  define([
    'app',
    'env',
    'can/util/can',

    'can/model'
  ], function (app, env, can) {
    can.Model.extend('app.App', {

    }, {

    });
    return app;
  });
}) (window);
