(function(window, require, define) {
  'use strict';
  return define([
    'app'
  ], function(app) {
      app.form = {
        name         : 'app/form',
        init         : function() {}
      };
      return app.form;
  });
}) (window, require, define);
